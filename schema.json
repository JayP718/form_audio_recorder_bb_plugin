{
  "type": "component",
  "metadata": {},
  "schema": {
    "name": "form_audio_recorder",
    "friendlyName": "Form Audio Recorder",
    "description": "Allows users to record voice clips and download the result in browser.",
    "icon": "VoiceOver",
    "settings": [
      {
        "type": "boolean",
        "key": "disabled",
        "label": "Disabled"
      },
      {
        "type": "text",
        "key": "filename",
        "label": "Filename",
        "defaultValue": "recording"
      },
      {
        "type": "field/attachment",
        "key": "field",
        "label": "Field",
        "required":true
      },
      {
      "type": "select",
      "label": "Audio Format",
      "key": "type",
      "options": [
        {
          "label": "OGG",
          "value": "audio/ogg"
        },
        {
          "label": "WAV",
          "value": "audio/wav"
        }
      ],
      "defaultValue": "audio/wav"
      }
    ]
  }
}
